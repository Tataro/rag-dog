// Route marker for /chat (no conversation selected). The chat workspace — sidebar +
// thread — is rendered by chat/layout.tsx, which derives the active conversation from
// the pathname.
export default function ChatPage() {
  return null;
}
