// Route marker for /chat/<id>. The chat workspace is rendered by chat/layout.tsx,
// which reads the active conversation id from the pathname and loads it.
export default function ChatConversationPage() {
  return null;
}
